# Production docker-compose using pre-built images from GitHub Container Registry
# Usage: docker compose -f docker-compose.prod.yml up -d
#
# Pull latest images:
#   docker pull ghcr.io/lumose-health/glycemicgpt-api:latest
#   docker pull ghcr.io/lumose-health/glycemicgpt-web:latest
#   docker pull ghcr.io/lumose-health/glycemicgpt-sidecar:latest
#
# Or use a specific version:
#   IMAGE_TAG=0.1.2 docker compose -f docker-compose.prod.yml up -d

services:
  db:
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-glycemicgpt}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      POSTGRES_DB: ${POSTGRES_DB:-glycemicgpt}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-glycemicgpt}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: >
      sh -c '[ -n "$$REDIS_PASSWORD" ] && exec redis-server --requirepass "$$REDIS_PASSWORD" || exec redis-server'
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD:-}
    volumes:
      - redis_data:/data
    healthcheck:
      # Only authenticate when a password is set; an empty REDISCLI_AUTH makes
      # redis-cli attempt AUTH against a passwordless server (noisy stderr).
      test: ["CMD-SHELL", "if [ -n \"$$REDIS_PASSWORD\" ]; then export REDISCLI_AUTH=\"$$REDIS_PASSWORD\"; fi; redis-cli ping | grep -q PONG"]
      interval: 10s
      timeout: 5s
      retries: 5

  ai-sidecar:
    image: ghcr.io/lumose-health/glycemicgpt-sidecar:${IMAGE_TAG:-latest}
    restart: unless-stopped
    volumes:
      - sidecar_auth:/home/sidecar/.config
      - sidecar_codex:/home/sidecar/.codex
    environment:
      LOG_LEVEL: ${LOG_LEVEL:-info}
      # Required: the sidecar refuses to start without a key. Generate with:
      # openssl rand -hex 32
      SIDECAR_API_KEY: "${SIDECAR_API_KEY:?SIDECAR_API_KEY is required -- generate with openssl rand -hex 32}"
      # Sentry error monitoring (own project) -- empty default = disabled.
      # Runtime-only; never baked into the image (see PRIVACY.md).
      GLYCEMICGPT_SIDECAR_SENTRY_DSN: ${GLYCEMICGPT_SIDECAR_SENTRY_DSN:-}
      GLYCEMICGPT_SIDECAR_SENTRY_ENVIRONMENT: ${GLYCEMICGPT_SIDECAR_SENTRY_ENVIRONMENT:-production}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3456/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s

  api:
    image: ghcr.io/lumose-health/glycemicgpt-api:${IMAGE_TAG:-latest}
    restart: unless-stopped
    ports:
      - "${API_PORT:-8000}:8000"
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-glycemicgpt}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB:-glycemicgpt}
      # When REDIS_PASSWORD is set, override REDIS_URL in .env:
      # REDIS_URL=redis://:<your-redis-password>@redis:6379/0
      REDIS_URL: ${REDIS_URL:-redis://redis:6379/0}
      LOG_FORMAT: ${LOG_FORMAT:-json}
      SECRET_KEY: ${SECRET_KEY:?SECRET_KEY is required}
      AI_SIDECAR_URL: http://ai-sidecar:3456
      AI_SIDECAR_API_KEY: "${SIDECAR_API_KEY:?SIDECAR_API_KEY is required -- generate with openssl rand -hex 32}"
      # Sentry error monitoring (Sentry for Good) -- empty default = disabled.
      # Set only in the project's own dev/CI/staging; never in a distributed build.
      GLYCEMICGPT_SENTRY_DSN: ${GLYCEMICGPT_SENTRY_DSN:-}
      GLYCEMICGPT_SENTRY_ENVIRONMENT: ${GLYCEMICGPT_SENTRY_ENVIRONMENT:-production}
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      ai-sidecar:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  web:
    image: ghcr.io/lumose-health/glycemicgpt-web:${IMAGE_TAG:-latest}
    restart: unless-stopped
    ports:
      - "${WEB_PORT:-3000}:3000"
    environment:
      API_URL: http://api:8000
      # Sentry error monitoring (own project, SERVER-SIDE only) -- empty = disabled.
      # Runtime-only; never baked into the image or browser bundle (see PRIVACY.md).
      GLYCEMICGPT_WEB_SENTRY_DSN: ${GLYCEMICGPT_WEB_SENTRY_DSN:-}
      GLYCEMICGPT_WEB_SENTRY_ENVIRONMENT: ${GLYCEMICGPT_WEB_SENTRY_ENVIRONMENT:-production}
    depends_on:
      api:
        condition: service_healthy

volumes:
  postgres_data:
  redis_data:
  sidecar_auth:
  sidecar_codex:
